<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
	
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/locDetails" method="post">
          <input name="city" type="text" class="ghost-input" placeholder="Enter a City" required>
          <input type="submit" class="ghost-button" value="Get Details">
        </form>
		 <% if(weather !== null){ %>
          <p>The temperature is <%= weather %> in <%= city %> </p>
		  <p>Latitude:	<%= latitude %></p>
		  <p>Longitude:	<%= longitude %></p>
		  <form action="/sendMail" method="get">
		   <input name="email" type="email" class="ghost-input" placeholder="Enter your mail id" required>
		   <input type="submit" class="ghost-button" value="Send Mail">
		  
		  </form>
		 
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>